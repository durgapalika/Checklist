import { StyledText } from "./components";

const Header = () => {
  return (
    <StyledText variant="h3" data-testid="headerText">
      My checklist
    </StyledText>
  );
};

export default Header;
